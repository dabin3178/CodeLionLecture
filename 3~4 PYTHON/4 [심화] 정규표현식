<정규표현식>
^를 통해 시작을 알리고, $를 통해 끝을 알림

[a-zA-A0-9.+_-]+
a부터 z까지, A부터 Z까지, 0부터 9까지, ., +, _, - 가 1회 이상 반복된다.

@
@가 붙는다.

[a-zA-A0-9]+
a부터 z까지, A부터 Z까지, 0부터 9까지가 1회 이상 반복된다.

\.
.이 붙는다.

[a-zA-Z]{2,3}
a부터 z까지, A부터 Z까지가 최소 2회, 최대 3번 반복된다.
